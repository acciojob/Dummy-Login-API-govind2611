const React = require('react');
const LoginForm = require('./LoginForm');

const App = () => {
  return (
    <div>
      <h1>Login Form</h1>
      <LoginForm />
    </div>
  );
};

module.exports = App;
